import { SeguidoresPipe } from './seguidores-pipe';

describe('SeguidoresPipe', () => {
  it('create an instance', () => {
    const pipe = new SeguidoresPipe();
    expect(pipe).toBeTruthy();
  });
});
